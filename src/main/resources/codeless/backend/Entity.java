package ${packageName};

import java.io.Serializable;
${imports}

/**
 * @author codeless ${now}
 * @version ${version}
 */
public class ${className} implements Serializable {
${fields}
${constructor}
${methods}
}
