package ${packageName};

import java.io.Serializable;
${imports}

/**
 * @author codelee ${now}
 * @version ${version}
 */
public class ${className} implements Serializable {
${fields}
${constructor}
${methods}
}
